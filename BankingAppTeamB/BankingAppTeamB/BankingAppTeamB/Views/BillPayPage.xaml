<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BankingAppTeamB.Views.BillPayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BankingAppTeamB.Models"
    xmlns:converters="using:BankingAppTeamB.Converters"
    Background="#F7F7F7">

    <Page.Resources>
        <converters:StepToVisibilityConverter x:Key="StepToVisibility" />
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Spacing="20" Padding="24">

            <!-- STEP 1 -->
            <Border
                Padding="20"
                CornerRadius="12"
                Background="White"
                Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=1}">
                <StackPanel Spacing="14">

                    <TextBlock Text="Select Biller"
                               FontSize="22"
                               FontWeight="SemiBold" />

                    <AutoSuggestBox x:Name="SearchBox"
                                    PlaceholderText="Search billers..."
                                    Text="{Binding SearchQuery, Mode=TwoWay}"
                                    TextChanged="SearchBox_TextChanged" />

                    <ComboBox x:Name="CategoryCombo"
                              Header="Category"
                              SelectedValuePath="Content"
                              SelectedValue="{Binding SelectedCategory, Mode=TwoWay}"
                              SelectionChanged="CategoryCombo_SelectionChanged">
                        <ComboBoxItem Content="Utilities" />
                        <ComboBoxItem Content="Internet" />
                        <ComboBoxItem Content="Mobile" />
                        <ComboBoxItem Content="Insurance" />
                        <ComboBoxItem Content="Taxes" />
                        <ComboBoxItem Content="Government" />
                        <ComboBoxItem Content="Rent" />
                        <ComboBoxItem Content="Subscriptions" />
                        <ComboBoxItem Content="Telecom" />
                    </ComboBox>

                    <StackPanel Visibility="{Binding SavedBillersVisibility}" Spacing="8">
                        <TextBlock Text="Saved billers"
                                   FontSize="16"
                                   FontWeight="SemiBold" />

                        <ListView x:Name="SavedBillersList"
                                  ItemsSource="{Binding SavedBillers}"
                                  IsItemClickEnabled="True"
                                  ItemClick="SavedBillersList_ItemClick"
                                  SelectionMode="None"
                                  MaxHeight="180">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SavedBiller">
                                    <StackPanel Padding="6">
                                        <TextBlock Text="{Binding Nickname, FallbackValue=Saved Biller}"
                                                   FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Biller.Name}"
                                                   FontSize="14" />
                                        <TextBlock Text="{Binding Biller.Category}"
                                                   FontSize="12"
                                                   Opacity="0.7" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <TextBlock Text="All billers"
                               FontSize="16"
                               FontWeight="SemiBold" />

                    <ListView x:Name="BillersList"
                              ItemsSource="{Binding Billers}"
                              SelectedItem="{Binding SelectedBiller, Mode=TwoWay}"
                              IsItemClickEnabled="True"
                              ItemClick="BillersList_ItemClick"
                              SelectionMode="Single"
                              MaxHeight="320">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:Biller">
                                <StackPanel Padding="6">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Category}"
                                               FontSize="12"
                                               Opacity="0.7" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="Red"
                               TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Cancel"
                                Command="{Binding CancelCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- STEP 2 -->
            <Border
                Padding="20"
                CornerRadius="12"
                Background="White"
                Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=2}">
                <StackPanel Spacing="14">

                    <TextBlock Text="Enter Bill Details"
                               FontSize="22"
                               FontWeight="SemiBold" />

                    <StackPanel Spacing="4">
                        <TextBlock Text="Selected biller"
                                   FontSize="13"
                                   Opacity="0.7" />
                        <TextBlock Text="{Binding SelectedBillerName}"
                                   FontSize="18"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <TextBox Header="Biller reference"
                             Text="{Binding BillerReference, Mode=TwoWay}" />

                    <NumberBox x:Name="AmountBox"
                               Header="Amount"
                               Minimum="0"
                               SmallChange="1"
                               SpinButtonPlacementMode="Compact"
                               ValueChanged="AmountBox_ValueChanged" />

                    <ComboBox Header="Source account"
                              ItemsSource="{Binding Accounts}"
                              SelectedItem="{Binding SelectedAccount, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="models:Account">
                                <StackPanel Padding="4">
                                    <TextBlock Text="{Binding AccountName}"
                                               FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding IBAN}"
                                               FontSize="13" />
                                    <TextBlock FontSize="12" Opacity="0.7">
                                        <Run Text="{Binding Currency}" />
                                        <Run Text=" | Balance: " />
                                        <Run Text="{Binding Balance}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="Red"
                               TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Next"
                                Command="{Binding NextStepCommand}" />
                        <Button Content="Cancel"
                                Command="{Binding CancelCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- STEP 3 -->
            <Border
                Padding="20"
                CornerRadius="12"
                Background="White"
                Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=3}">
                <StackPanel Spacing="12">

                    <TextBlock Text="Review Bill Payment"
                               FontSize="22"
                               FontWeight="SemiBold" />

                    <StackPanel Spacing="4">
                        <TextBlock Text="Biller" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedBillerName}" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Reference" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding BillerReference}" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Amount" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ReviewAmountText}" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Fee" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ReviewFeeText}" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Source account" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedAccount.AccountName}" />
                        <TextBlock Text="{Binding SelectedAccount.IBAN}" />
                    </StackPanel>

                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="Red"
                               TextWrapping="Wrap" />

                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Pay"
                                Command="{Binding PayBillCommand}" />
                        <Button Content="Cancel"
                                Command="{Binding CancelCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- STEP 4 -->
            <Border
                Padding="20"
                CornerRadius="12"
                Background="White"
                Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=4}">
                <StackPanel Spacing="12">

                    <TextBlock Text="Payment Successful"
                               FontSize="22"
                               FontWeight="SemiBold" />

                    <TextBlock Text="Receipt number"
                               FontWeight="SemiBold" />

                    <TextBlock Text="{Binding ReceiptNumber}"
                               FontSize="18" />

                    <Button Content="Back to Home"
                            Command="{Binding CancelCommand}" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
    
    
    <!--<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BankingAppTeamB.Views.BillPayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BankingAppTeamB.Models"
    xmlns:converters="using:BankingAppTeamB.Converters">

    <Page.Resources>
        <converters:StepToVisibilityConverter x:Key="StepToVisibility"/>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Spacing="20" Padding="30">

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=1}"
                        Spacing="12">

                <TextBlock Text="Select Biller"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Saved Billers"
                           FontSize="16"
                           FontWeight="SemiBold"/>

                <ListView ItemsSource="{Binding SavedBillers}"
                          SelectionMode="Single"
                          IsItemClickEnabled="True"
                          ItemClick="SavedBillersListView_ItemClick"
                          MaxHeight="180">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:SavedBiller">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind Nickname, Mode=OneWay}"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Biller.Name, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind Biller.Category, Mode=OneWay}"
                                           FontSize="12"
                                           Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <AutoSuggestBox PlaceholderText="Search billers..."
                                Text="{Binding SearchQuery, Mode=TwoWay}"
                                TextChanged="SearchBox_TextChanged"/>

                <ComboBox Header="Category"
                          PlaceholderText="Select category"
                          SelectedValuePath="Content"
                          SelectedValue="{Binding SelectedCategory, Mode=TwoWay}"
                          SelectionChanged="CategoryComboBox_SelectionChanged">
                    <ComboBoxItem Content="Utilities"/>
                    <ComboBoxItem Content="Internet"/>
                    <ComboBoxItem Content="Mobile"/>
                    <ComboBoxItem Content="Insurance"/>
                    <ComboBoxItem Content="Taxes"/>
                    <ComboBoxItem Content="Education"/>
                    <ComboBoxItem Content="Healthcare"/>
                </ComboBox>

                <ListView ItemsSource="{Binding Billers}"
                          SelectedItem="{Binding SelectedBiller, Mode=TwoWay}"
                          IsItemClickEnabled="True"
                          ItemClick="BillersListView_ItemClick"
                          MaxHeight="300">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Biller">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Category, Mode=OneWay}"
                                           FontSize="12"
                                           Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        HorizontalAlignment="Left"/>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=2}"
                        Spacing="12">

                <TextBlock Text="Payment Details"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Selected Biller"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding SelectedBiller.Name}"/>

                <TextBox Header="Biller Reference"
                         PlaceholderText="Enter invoice/customer reference"
                         Text="{Binding BillerReference, Mode=TwoWay}"/>

                <ToggleSwitch Header="Pay in full"
                              IsOn="{Binding IsPayInFull, Mode=TwoWay}"
                              Toggled="PayInFullToggle_Toggled"/>

                <StackPanel x:Name="AmountContainer">
                    <NumberBox Header="Amount"
                               Minimum="0"
                               SmallChange="1"
                               SpinButtonPlacementMode="Compact"
                               Value="{Binding AmountValue, Mode=TwoWay}"/>
                </StackPanel>

                <ComboBox Header="Source Account"
                          ItemsSource="{Binding Accounts}"
                          SelectedItem="{Binding SelectedAccount, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="models:Account">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind AccountName, Mode=OneWay}"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind IBAN, Mode=OneWay}"
                                           FontSize="12"
                                           Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Next"
                            Command="{Binding NextStepCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=3}"
                        Spacing="10">

                <TextBlock Text="Confirm Payment"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Biller:"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedBiller.Name}"/>

                <TextBlock Text="Reference:"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding BillerReference}"/>

                <TextBlock Text="Amount:"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding Amount}"/>

                <TextBlock Text="Fee:"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding Fee}"/>

                <TextBlock Text="From Account:"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedAccount.IBAN}"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Pay"
                            Command="{Binding PayBillCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=4}"
                        Spacing="12">

                <TextBlock Text="Payment Successful!"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Receipt Number:"
                           FontWeight="Bold"/>

                <TextBlock Text="{Binding ReceiptNumber}"
                           FontSize="18"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
    
    
   <?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BankingAppTeamB.Views.BillPayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BankingAppTeamB.Models"
    xmlns:converters="using:BankingAppTeamB.Converters">

    <Page.Resources>
        <converters:StepToVisibilityConverter x:Key="StepToVisibility"/>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Spacing="20" Padding="30">

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=1}"
                        Spacing="12">
                <TextBlock Text="Select Biller"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Saved Billers"
                           FontSize="16"
                           FontWeight="SemiBold"/>

                <ListView ItemsSource="{Binding SavedBillers}"
                          SelectionMode="Single"
                          IsItemClickEnabled="True"
                          ItemClick="SavedBillersListView_ItemClick"
                          MaxHeight="180">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:SavedBiller">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind Nickname, Mode=OneWay}" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Biller.Name, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind Biller.Category, Mode=OneWay}" FontSize="12" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <AutoSuggestBox PlaceholderText="Search billers..."
                                Text="{Binding SearchQuery, Mode=TwoWay}"
                                TextChanged="SearchBox_TextChanged"/>

                <ComboBox Header="Category"
                          PlaceholderText="Select category"
                          SelectedValuePath="Content"
                          SelectedValue="{Binding SelectedCategory, Mode=TwoWay}"
                          SelectionChanged="CategoryComboBox_SelectionChanged">
                    <ComboBoxItem Content="Utilities"/>
                    <ComboBoxItem Content="Internet"/>
                    <ComboBoxItem Content="Mobile"/>
                    <ComboBoxItem Content="Insurance"/>
                    <ComboBoxItem Content="Taxes"/>
                    <ComboBoxItem Content="Education"/>
                    <ComboBoxItem Content="Healthcare"/>
                </ComboBox>

                <ListView ItemsSource="{Binding Billers}"
                          SelectedItem="{Binding SelectedBiller, Mode=TwoWay}"
                          IsItemClickEnabled="True"
                          ItemClick="BillersListView_ItemClick"
                          MaxHeight="300">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Biller">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind Category, Mode=OneWay}" FontSize="12" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        HorizontalAlignment="Left"/>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=2}"
                        Spacing="12">
                <TextBlock Text="Payment Details"
                           FontSize="20"
                           FontWeight="SemiBold"/>

                <TextBlock Text="Selected Biller"
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding SelectedBiller.Name}"/>

                <TextBox Header="Biller Reference"
                         PlaceholderText="Enter invoice/customer reference"
                         Text="{Binding BillerReference, Mode=TwoWay}"/>

                <ToggleSwitch Header="Pay in full"
                              IsOn="{Binding IsPayInFull, Mode=TwoWay}"
                              Toggled="PayInFullToggle_Toggled"/>

                <StackPanel x:Name="AmountContainer">
                    <NumberBox Header="Amount"
                               Minimum="0"
                               SmallChange="1"
                               SpinButtonPlacementMode="Compact"
                               Value="{Binding Amount, Mode=TwoWay}"/>
                </StackPanel>

                <ComboBox Header="Source Account"
                          ItemsSource="{Binding Accounts}"
                          SelectedItem="{Binding SelectedAccount, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="models:Account">
                            <StackPanel Spacing="2">
                                <TextBlock Text="{x:Bind AccountName, Mode=OneWay}" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind IBAN, Mode=OneWay}" FontSize="12" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Next"
                            Command="{Binding NextStepCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=3}"
                        Spacing="10">

                <TextBlock Text="Confirm Payment" FontSize="20" FontWeight="SemiBold"/>

                <TextBlock Text="Biller:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedBiller.Name}"/>

                <TextBlock Text="Reference:" FontWeight="Bold"/>
                <TextBlock Text="{Binding BillerReference}"/>

                <TextBlock Text="Amount:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Amount}"/>

                <TextBlock Text="Fee:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Fee}"/>

                <TextBlock Text="From Account:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedAccount.IBAN}"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Pay"
                            Command="{Binding PayBillCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=4}"
                        Spacing="12">
                <TextBlock Text="Payment Successful!" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ReceiptNumber}" FontSize="18"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
    
<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BankingAppTeamB.Views.BillPayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:BankingAppTeamB.Models"
    xmlns:converters="using:BankingAppTeamB.Converters">

    <Page.Resources>
        <converters:StepToVisibilityConverter x:Key="StepToVisibility"/>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Spacing="20" Padding="30">

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=1}"
                        Spacing="12">
                <TextBlock Text="Select Biller" FontSize="20" FontWeight="SemiBold"/>

                <TextBox PlaceholderText="Search..."
                         Text="{Binding SearchQuery, Mode=TwoWay}"/>

                <ComboBox PlaceholderText="Category"
                          SelectedValuePath="Content"
                          SelectedValue="{Binding SelectedCategory, Mode=TwoWay}">
                    <ComboBoxItem Content="Utilities"/>
                    <ComboBoxItem Content="Internet"/>
                    <ComboBoxItem Content="Mobile"/>
                    <ComboBoxItem Content="Insurance"/>
                    <ComboBoxItem Content="Taxes"/>
                </ComboBox>

                <Button Content="Search"
                        Command="{Binding SearchCommand}"/>

                <ListView ItemsSource="{Binding Billers}"
                          SelectedItem="{Binding SelectedBiller, Mode=TwoWay}"
                          MaxHeight="300">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Biller">
                            <TextBlock Text="{x:Bind Name}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Next"
                            Command="{Binding NextStepCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=2}"
                        Spacing="12">
                <TextBlock Text="Payment Details" FontSize="20" FontWeight="SemiBold"/>

                <CheckBox Content="Pay in full"
                          IsChecked="{Binding IsPayInFull, Mode=TwoWay}"/>

                <TextBox Header="Reference"
                         Text="{Binding BillerReference, Mode=TwoWay}"
                         Visibility="{Binding IsPayInFull, Converter={StaticResource InverseBoolToVisibility}}"/>

                <NumberBox x:Name="AmountBox"
                           Header="Amount"
                           Minimum="0"
                           SmallChange="1"
                           SpinButtonPlacementMode="Compact"
                           ValueChanged="AmountBox_ValueChanged"/>

                <TextBlock Text="Select Source Account"/>

                <ComboBox ItemsSource="{Binding Accounts}"
                          SelectedItem="{Binding SelectedAccount, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="models:Account">
                            <StackPanel>
                                <TextBlock Text="{x:Bind IBAN}"/>
                                <TextBlock Text="{x:Bind AccountName}" FontSize="12" Opacity="0.7"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Next"
                            Command="{Binding NextStepCommand}"/>

                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=3}"
            Spacing="10">

                <TextBlock Text="Confirm Payment" FontSize="20" FontWeight="SemiBold"/>

                <TextBlock Text="Biller:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedBiller.Name}"/>

                <TextBlock Text="Amount:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Amount}"/>

                <TextBlock Text="Fee:" FontWeight="Bold"/>
                <TextBlock Text="{Binding Fee}"/>

                <TextBlock Text="From Account:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedAccount.IBAN}"/>

                <TextBlock Text="{Binding ErrorMessage}"
               Foreground="Red"
               TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button Content="Pay"
                Command="{Binding PayBillCommand}"/>

                    <Button Content="Cancel"
                Command="{Binding CancelCommand}"/>
                </StackPanel>

            </StackPanel>

            <StackPanel Visibility="{Binding CurrentStep, Converter={StaticResource StepToVisibility}, ConverterParameter=4}"
                        Spacing="12">
                <TextBlock Text="Payment Successful!" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding ReceiptNumber}" FontSize="18"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>-->
